# ES6 Enhancement

## 1. ES6 enhanced object literals :

      - inside our object , we just write a property name to refer to an outside object . it should be the same name of the object.

## 2. Writting methods or functions :

      - ES6 allows us to write fn without function keyword and without (:) colon . so the property name then the parenthes ( , ) of the fn or the method. which is easy syntax.

## 3. Compute property name :

      -Compute the property names instead of having to write them out manually and literally
